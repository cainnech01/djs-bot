const {Client, Interaction} = require('discord.js');
const AutoRole = require('../../models/AutoRole');

module.exports = {
    name: 'autorole-configure',
    description: 'Configre the auto-role in server',
    options: {
        
    }
}